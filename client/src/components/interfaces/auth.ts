export interface signupDataType {
    full_name: string,
    email?: string,
    phone?: string,
    password: string
}
export interface loginDataType {
    username?: string,
    email?: string,
    phone?: string,
    password: string
}